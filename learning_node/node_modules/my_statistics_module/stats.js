exports.mean = function(set) {
	n = set.length;
	if (n == 0) {
		return 0;
	}
	else {
		var sum = set.reduce(function(accumulator, current){
			return accumulator + current;
		})
		return sum/n;
	}
}
